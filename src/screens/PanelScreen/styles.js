import styles2 from '../RegisterScreen/styles';
import styles3 from '../../components/FormTextInput/styles';

const container = {
  ...styles2.container,
  width: '100%',
  minHeight: '97vh',
};

const button = {
  ...styles2.button,
};

const input = {
  ...styles3.input,
  marginBottom: '25px',
};

const label = {
  ...styles3.label,
};

const title = {
  ...styles2.title,
};

const styles = { container, button, input, label, title };

export default styles;
