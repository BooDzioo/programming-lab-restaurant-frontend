const container = {
  width: '100%',
  height: '93vh',
  backgroundColor: 'red',
};

const image = {
  with: '100%',
  height: '100%',
  resizeMode: 'cover',
};

const imgContainer = {
  width: '100%',
  height: '100%',
};

const styles = { container, image, imgContainer };

export default styles;
